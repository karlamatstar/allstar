FROM python:3.12-slim

WORKDIR /srv/app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY src/ ./src/
ENV PYTHONPATH=/srv/app/src
EXPOSE 8000
CMD ["uvicorn", "allstar.ai_agent.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
