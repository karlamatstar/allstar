# QA 로그·보고서 자동화 기준

> 최초 작성일: 2026-07-17
> 상태: **공통 누적 로그·최신 실행 요약·manifest·도메인별 정식 보고서 분리 구현 및 비API 검증 완료**

> 후속 구현사항: 직접 부하 K6 5개는 사용자용 보고서에서 제외했고 AI 테스트케이스 QA 탭을 추가했다. 장애·기능 검증 결함 보고서와 서버 연결 성능 보고서는 제외 대상이 아니며 그대로 유지한다. 상세 기준은 `QA_AI_TESTCASE_TAB_AND_K6_REPORT_POLICY.md`를 따른다.

## 목적

품질검사 관리 GUI의 모든 시험이 같은 방식으로 실행 증적을 남기도록 한다. 원문 로그는 실행할 때마다 새 파일로 누적하고, 보고서 대상 시험의 공통 실행 요약은 가장 최근 실행 결과로 갱신한다. 공통 실행 요약과 도메인별 정식 결과 보고서는 역할을 분리한다.

## 적용 범위

- AI 상담 품질검사 8개 시험
- 고객 의견 분석 품질검사 전체 비AI 검사와 단위 테스트
- 고객 의견 분석 A~D 에이전트 교차 테스트
- 완료·실패·사용자 중지·시작 실패 실행

통합 대시보드에는 QA 공통 실행 목록이나 필터 화면을 새로 만들지 않는다. 공통 이벤트와 원문 로그는 사용자 화면을 위한 데이터가 아니라 실행 증적과 문제 추적을 위해 계속 보존한다.

## K6 보고서 범위 구분

- 기본 동작·일반 부하·무작위 요청·한계 부하·순간 급증 시험은 직접 부하 K6 5종으로 분류한다.
- 직접 부하 K6 5종은 별도 사용자용 Markdown·Word·PNG 보고서를 생성하지 않고 원문 로그·K6 요약·Prometheus·Grafana를 유지한다.
- 장애·기능 검증 시험은 K6 장애 모의와 Pytest를 결합하므로 결함 Markdown·Word 보고서를 계속 생성한다.
- 서버 연결 성능 종합 시험은 K6 1명·10명·25명 단계별 결과로 성능 Markdown 보고서를 계속 생성한다.
- 기존 AI·VOC 정식 품질 보고서와 결함 기록은 이 변경으로 삭제하거나 축소하지 않는다.

## 저장 규칙

```text
_OUTPUT/logs/qa/qa_runs.jsonl
_OUTPUT/logs/qa/runs/{test_id}/{run_id}.log
_OUTPUT/logs/qa/k6/{test_id}/{run_id}_summary.json

_OUTPUT/reports/qa/latest/{test_id}.md
_OUTPUT/reports/qa/latest/latest_report.md
_OUTPUT/reports/manifests/qa/{test_id}.json
_OUTPUT/reports/manifests/qa/latest.json
```

- `run_id`는 실행 시각과 임의 식별자를 결합해 중복을 방지한다.
- `qa_runs.jsonl`은 시작·종료 이벤트를 계속 추가한다.
- 실행별 `.log`는 명령, 설정, 전체 콘솔 출력, 종료 상태를 보존하며 덮어쓰지 않는다.
- k6 원본 요약도 실행별 파일로 누적한다.
- 직접 부하 K6 5종을 제외한 시험별 Markdown은 정식 품질 보고서가 아니라 최근 실행 상태를 보여주는 보조 요약이며 같은 시험의 최근 결과로 덮어쓴다.
- `latest_report.md`는 직접 부하 K6 5종을 제외하고 가장 마지막에 끝난 QA 실행 요약으로 덮어쓴다.
- 직접 부하 K6 5종을 제외한 manifest는 시험별 최신본과 전체 최신본만 유지한다. 대시보드는 폴더나 파일명을 추측하지 않고 manifest의 `sources`와 `report`를 읽는다.
- VOC A~D 실행 요약은 해당 프로필의 정식 `quality_score_report.md`를 함께 연결한다.
- 단계별 성능·장애 시험이 사용하는 `ops/performance/results/`는 로컬 중간 산출물이므로 Git에서 제외하고, 정식 누적 로그와 최신 보고서는 `_OUTPUT` 규칙을 따른다.

## 공통 기록 항목

- 실행 식별자, 시험 ID와 시험명
- 실행 명령과 실행 조건
- 시작·완료 시각과 총 실행시간
- 완료·실패·사용자 중지·시작 실패 상태
- 프로세스 종료 코드와 오류 내용
- 원문 로그, k6 요약, 최신 보고서 연결 정보

부하 시험은 최대 가상 인원(VU)과 실행 시간을 기록한다. A~D 에이전트 교차 테스트는 프로필, 실행 시점의 전체 테스트케이스 수와 실제 AI 평가 대상 수를 기록한다.

서버 연결 성능 종합 시험은 `1명 → 10명 → 25명`의 실행 단계, 단계별 독립 실행 방식, 단계 사이 5초 안정화 대기, 정상 완료 기준 총 36건의 실제 채팅 요청을 기록한다. 성능 원본 데이터의 각 요청에는 단계 식별자를 저장하고, 최신 성능 보고서는 시작 시각 구간이 아니라 이 식별자를 기준으로 단계별 결과를 집계한다.

VOC 테스트케이스 정식 보고서의 단일 실행·로그·표·그래프 기준은 `VOC_TESTCASE_REPORT_AUTOMATION.md`를 따른다.

AI Agent 실제 챗봇 대화의 백그라운드 채점과 매 채팅 최신 실시간 보고서 자동 갱신은 QA GUI 공통 실행 요약과 별개이며 `AI_AGENT_LIVE_REPORT_AUTOMATION.md`를 따른다.

## 결과 지표

k6 요약이 존재하면 다음 값을 파싱한다. 직접 부하 K6 5종은 완료 이벤트와 원문 로그에 기록하고, 보고서를 유지하는 복합 시험은 보고서와 manifest에도 기록한다.

- 전체 HTTP 요청 수
- 요청 실패율
- 평균 응답시간
- p95 응답시간
- 검사 성공·실패 수

K6 1.x 종료 JSON의 `metrics.*.values` 구조와 K6 2.x의 `metrics.*` 직접 속성 구조를 모두 지원한다. 종료 요약 파일은 보고서 생성용 집계 자료이고, Grafana 실시간 그래프는 별도 `experimental-prometheus-rw` 출력으로 Prometheus에 전송한다. 두 출력은 동시에 사용하며 실행별 `testid`로 시계열을 구분한다.

pytest 출력이 존재하면 통과·실패·오류·제외·선택 제외 건수를 기록한다. 별도 수치가 없는 시험도 실행 상태, 실행시간, 종료 코드와 원문 로그를 반드시 남긴다.

## 실패 및 중지 처리

직접 부하 K6 5종을 제외한 시험은 실패하거나 사용자가 중지해도 확보된 결과로 공통 실행 요약을 생성한다. 직접 부하 K6 5종은 성공·실패·중지와 관계없이 원문 로그·K6 JSON·완료 이벤트만 남기고 사용자용 보고서를 만들지 않는다. k6 요약이 없거나 출력이 일부만 존재해도 원문 로그와 종료 상태는 보존한다. 최신 보고서 쓰기는 임시 파일을 만든 뒤 교체하는 방식으로 처리해 작성 도중의 불완전한 파일 노출을 줄인다.

AI 테스트케이스 품질 시험은 모든 사례가 정상 완료된 뒤에만 정식 JSON·CSV·Markdown·PNG와 manifest를 갱신한다. 사례 처리 도중 실패하거나 사용자가 중지하면 기존의 마지막 정상 정식 보고서와 그래프를 유지하고 새 실행의 터미널·원문 로그만 누적한다.

AI 테스트케이스 배치 원본은 정식 보고서 manifest가 존재하는 완료 실행을 기준으로 최근 5개를 원본 JSON으로 유지하고 그보다 오래된 원본은 GZIP으로 자동 압축한다. 최신·이력 보고서, CSV·PNG와 manifest는 압축하지 않으며 재생성기는 원본과 GZIP을 함께 읽는다.

VOC A~D 테스트케이스도 동일한 정상 완료 보호 규칙을 사용한다. 케이스별 중간 결과는 실행별 `report_draft/`에만 저장하고 전체 실행·Judge 검증이 성공한 경우에만 프로필 최신 정식 보고서·그래프·manifest와 A~D 종합 비교를 갱신한다. 중지·서버 종료·API 실패·파이프라인 실패·파싱 실패는 실행 로그와 초안에 남기되 마지막 정상 정식 결과를 덮어쓰지 않는다.

VOC 테스트케이스는 프로필별 최근 완료 실행 5개의 원본을 유지하고 완료 manifest가 있는 이전 실행 원본만 압축한다. 보고서·그래프·manifest와 현재 진행 파일은 압축하지 않는다. 공통 세부 기준은 `LOG_RETENTION_AND_COMPRESSION.md`를 따른다.

프로세스 종료 코드가 0이어도 `Judge 실패`, 외부 AI API 오류, `미평가(API 실패)`가 출력되면 보고서 상태를 `경고 포함 완료`로 기록한다. A~D 프로필 실행기는 각 사례의 Judge CSV를 확인하고 미평가 결과가 있으면 실패 종료 코드를 반환한다.

## 실제 외부 AI 실행 범위

사용자가 QA GUI 또는 통합 대시보드에서 A~D 실행 버튼을 직접 누르면 실행 시점에 등록된 테스트케이스 전체를 사용한다. 확인창에는 전체 케이스 수, 실제 AI 평가 대상 수, 프로필과 예상 호출 범위를 표시한다.

개발·통합 검증 명령은 실행 목적, 대상 프로필, 선택 사례 수와 예상 외부 호출 범위를 먼저 확인한다. `--case-id`를 사용하면 필요한 사례를 명시적으로 선택할 수 있지만 제품 정책상 2건으로 고정하지 않는다. QA GUI와 통합 대시보드의 실행 버튼은 실행 시점에 등록된 전체 테스트케이스를 사용한다.

## 2026-07-17 실제 A 프로필 검증

- 당시 실행 범위: A 프로필의 `TC-01`, `TC-02` 두 사례
- 생성 설정: OpenAI `gpt-5.6-luna`, 추론 없음
- 독립 평가 설정: Anthropic `claude-sonnet-5`, 추론 낮음
- 두 사례의 6개 에이전트 파이프라인과 OpenAI 생성 단계는 완료됐다.
- Anthropic 인증 오류 `401`로 두 사례의 독립 평가는 `미평가(API 실패)`가 됐다.
- 공통 최신 보고서는 해당 실행을 `경고 포함 완료`로 표시하고 누적 원문 로그를 연결한다.
- 인증 오류는 키 값을 문서나 로그에 복사하지 않고 오류 유형만 기록했다.
- Retriever가 호스트의 Windows 기본 CSV 경로를 Docker 내부에서 찾지 못하던 문제는 에이전트 로컬 기본 `voc.csv`로 안전하게 보정하도록 수정했다.
- `llm_judge` 모듈 직접 실행 시 누락됐던 `sys` import를 보완했다.
- 이후 OpenAI·Anthropic 인증 조회는 모두 성공했다. 다만 구조 변경 과정에서는 실제 두 사례를 다시 실행하지 않았다.
